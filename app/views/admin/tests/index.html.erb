<h1><%= t('.header') %></h1>
<table>
  <tr>
    <td><%= t('.name') %></td>
    <td><%= t('.category') %></td>
    <td><%= t('.level') %></td>
    <td><%= t('.questions') %></td>
    <td colspan="2", align="center" > <%= t('.actions') %></td>
  </tr>
<%=render @tests%>
</table>

<%= link_to  t('.create_test'), new_admin_test_path %>
